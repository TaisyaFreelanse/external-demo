# External API Demo - Standalone Playground

Отдельный демо-сайт для тестирования внешнего API управления мероприятиями.

## 🎯 Назначение

Это standalone приложение (отдельный мини-сайт), предназначенное для:

- Тестирования внешнего API создания и публикации мероприятий
- Демонстрации работы API внешним партнёрам
- Использования как "песочница" для разработчиков

## 🚀 Быстрый старт

### Установка зависимостей

```bash
npm install
```

### Настройка API

Создайте файл `.env` на основе `.env.example`:

**Windows:**
```bash
copy .env.example .env
```

**Linux/Mac:**
```bash
cp .env.example .env
```

Отредактируйте `.env` и укажите URL основного API:

```env
API_BASE_URL=http://localhost:3000
```

Для продакшена:

```env
API_BASE_URL=https://api.example.com
```

### Запуск в режиме разработки

```bash
npm run dev
```

Приложение будет доступно по адресу: `http://localhost:3000` (или другой порт, если 3000 занят)

### Сборка для продакшена

```bash
npm run build
```

### Предпросмотр продакшен-сборки

```bash
npm run preview
```

### Генерация статического сайта

```bash
npm run generate
```

## 📋 Функциональность

### 1. Создание черновика мероприятия

Форма позволяет создать новое мероприятие через внешний API:

- Все поля обязательны для заполнения
- Валидация выполняется на сервере
- Поддержка часовых поясов (IANA)
- Управление контрольными точками (t0, ti10, ti20, ti30, ti40, ti50)

### 2. Обновление черновика

Для обновления существующего черновика:

- Укажите ID мероприятия в поле "ID мероприятия"
- Заполните остальные поля
- Нажмите "Обновить черновик"

### 3. Публикация черновика

После создания черновика появляется форма публикации:

- Укажите ID мероприятия и Producer Code
- Нажмите "Опубликовать мероприятие"

### 4. Просмотр результатов

- **Ответ сервера**: JSON-ответ от API с данными созданного/обновлённого мероприятия
- **Ошибки валидации**: Список ошибок в читаемом формате (без JSON-структуры)

## 🔧 Технические детали

### Технологии

- **Nuxt 3** - Vue.js фреймворк с SSR поддержкой
- **Vue 3** - Прогрессивный JavaScript фреймворк
- **Tailwind CSS** - Utility-first CSS фреймворк для стилизации
- **PostCSS** - Инструмент для обработки CSS (настроен в nuxt.config.ts)

### Структура проекта

```
external-demo/
├── assets/
│   └── css/
│       └── main.css          # Основные стили с поддержкой Tailwind
├── pages/
│   └── index.vue             # Главная страница с формами API
├── app.vue                    # Корневой компонент
├── nuxt.config.ts             # Конфигурация Nuxt (включая PostCSS)
├── tailwind.config.js         # Конфигурация Tailwind CSS
├── package.json               # Зависимости проекта
├── .env.example               # Пример файла переменных окружения
├── .env                       # Файл переменных окружения (создать из .env.example)
├── .gitignore                 # Git ignore файл
└── README.md                  # Документация
```

**Примечание:** Конфигурация PostCSS находится в `nuxt.config.ts` (опция `postcss`), как рекомендовано в Nuxt 3.

### API Endpoints

Приложение использует следующие endpoints основного API:

- `POST /api/external/events` - Создание/обновление черновика
- `POST /api/external/events/publish` - Публикация черновика

## 🌐 Развёртывание

### Вариант 1: Статический хостинг

1. Соберите проект: `npm run generate`
2. Загрузите содержимое папки `.output/public` на статический хостинг (Netlify, Vercel, GitHub Pages и т.д.)

### Вариант 2: Node.js сервер

1. Соберите проект: `npm run build`
2. Запустите: `npm run preview`
3. Настройте reverse proxy (nginx) для работы на отдельном домене

### Вариант 3: Docker

Создайте `Dockerfile`:

```dockerfile
FROM node:20-alpine

WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

EXPOSE 3000
CMD ["node", ".output/server/index.mjs"]
```

## ⚙️ Конфигурация

### Переменные окружения

Проект использует следующие переменные окружения (в файле `.env`):

- `API_BASE_URL` - Базовый URL основного API (по умолчанию: `http://localhost:3000`)

### Nuxt конфигурация

Основные настройки находятся в `nuxt.config.ts`:

- **compatibilityDate**: `2025-11-14` - Дата совместимости для Nitro
- **PostCSS**: настроен в `nuxt.config.ts` (вместо отдельного файла)
- **Tailwind CSS**: автоматически настраивается модулем `@nuxtjs/tailwindcss`
- **SSR**: отключен (`ssr: false`) для упрощения развёртывания

## 🔒 Безопасность

- Это демо-приложение предназначено только для тестирования
- Не используйте его в продакшене без дополнительной защиты
- Рекомендуется добавить CORS настройки на основном API
- Рассмотрите возможность добавления API ключей для аутентификации
- Файл `.env` добавлен в `.gitignore` и не должен попадать в репозиторий

## 📝 Лицензия

Внутренний проект для тестирования API.

